import "products/ProductsIndex"; // It points to remotes field from ModuleFederationPlugin config in webpack config
import "cart/CartShow"; // It points to remotes field from ModuleFederationPlugin config in webpack config

console.log("Container!");
